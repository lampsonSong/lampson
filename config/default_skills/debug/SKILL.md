---
name: debug
description: 调试代码、排查错误、诊断疑难问题。简单 bug 走快速路径，疑难问题走深度诊断。
triggers:
- 报错
- 异常
- bug
- 修复
- 调试
- traceback
- 诊断
- 性能回归
- 难以复现
- diagnose
---

# Debug

严格按阶段执行，不跳步。

## 快速路径（已知报错）

适用于有明确报错信息或 traceback 的情况。

### Phase 1 — 复现

拿到明确的报错信息或异常行为。没有复现就不要改代码。

### Phase 2 — 定位

- 优先看 traceback 最后一行（直接原因）
- 向上追溯调用栈找根本原因
- 检查变量类型和值是否符合预期
- 必要时插入 print/logging 辅助排查
- 只做只读探测（ls、cat、which、env），不要盲目全盘搜索

### Phase 3 — 修复

- 在项目源码中直接修复，不留临时补丁
- 修复后跑验证，确认 bug 不再复现

## 深度诊断（疑难问题）

适用于没有明确报错、性能回归、难以复现的情况。

### Phase 1 — 建立反馈循环

**这是核心技能。** 如果有快速、确定性的 pass/fail 信号，你就能找到原因。

按优先级尝试：

1. **失败的测试** — 写一个能复现 bug 的测试
2. **HTTP 脚本** — curl 对着 dev server 跑
3. **CLI 调用** — 固定输入，diff 输出
4. **最小化复现** — 砍掉无关部分，缩到最小
5. **二分定位** — git bisect 或逐步排除
6. **差分对比** — 旧版本 vs 新版本，同一输入 diff 输出

在这个阶段投入不成比例的精力。不要放弃。

### Phase 2 — 最小化

把复现条件缩到最小：最少的数据、最短的路径、最简单的场景。

### Phase 3 — 假设

基于复现条件，列出可能的原因。按可能性排序。

### Phase 4 — 验证

逐个验证假设。用 instrument（日志、断点、计数器）而非猜测。

### Phase 5 — 修复

确认根因后修复。修复后：

1. 确认原 bug 复现步骤不再触发
2. 写回归测试防止再次出现
3. 清理调试产物

## 收尾（两种路径都要做）

- 清理调试过程中产生的临时文件
- 如果修复逻辑有通用性，考虑沉淀到 skill

## 禁止

- 禁止用临时脚本（/tmp/xxx.py）绕过问题
- 禁止用 workaround、临时 patch 掩盖症状
- 同一任务连续失败 2 次以上，停下来分析根因
- 必须找到根本原因，用最彻底的方式修复
