# 任务卡：Compaction 两阶段压缩 + 移除 Session Summary

请阅读 `docs/compaction-design.md` 了解完整设计。本任务卡只列出需要改动的代码。

## 任务 1：compaction.py — 两阶段压缩

**文件**：`src/core/compaction.py`

### 改动 1.1：CompactionConfig 添加新配置项

```python
@dataclass
class CompactionConfig:
    # ... 现有字段不动 ...
    keep_recent_n: int = 3               # 保留最近 N 轮不做摘要
    summary_trigger_ratio: float = 0.5   # 阶段一后仍超过此比例则触发阶段二
```

### 改动 1.2：Compactor.compact() 添加阶段二逻辑

在阶段一（分类归档）完成后，检查压缩后长度：

```python
def compact(self, messages, session_store=None, session_id=""):
    # ... 现有的阶段一逻辑 ...
    
    # 阶段一完成后
    remaining = _build_remaining_messages(messages, decisions, tool_refs, self.config.keep_recent_n)
    
    # 阶段二：检查是否需要摘要压缩
    original_tokens = _estimate_messages_tokens(
        [m for m in messages if m.get("role") != "system"]
    )
    remaining_tokens = _estimate_messages_tokens(remaining)
    
    if original_tokens > 0 and remaining_tokens > original_tokens * self.config.summary_trigger_ratio:
        # 需要第二层压缩
        remaining = self._summarize_keep_messages(remaining, messages, original_tokens)
    
    return CompactionResult(success=True, messages_kept=remaining, ...)
```

### 改动 1.3：新增 _summarize_keep_messages 方法

```python
def _summarize_keep_messages(self, remaining, original_messages, original_tokens):
    """阶段二：对除最近 N 轮外的 keep 消息做 LLM 分组摘要。"""
    keep_recent_n = self.config.keep_recent_n
    
    # 分离：最近 N 轮 vs 其余
    # "一轮" = 一条 user 消息 + 对应的 assistant + tool 消息
    recent_messages, older_messages = _split_recent_turns(remaining, keep_recent_n)
    
    if not older_messages:
        return remaining  # 没有需要摘要的
    
    # 跳过已有的 compaction summary
    older_messages = [m for m in older_messages if not m.get("is_compaction_summary")]
    
    if not older_messages:
        return remaining
    
    # LLM 生成摘要
    summary_text = self._generate_summary(older_messages)
    
    # 构造 summary message
    summary_msg = {
        "role": "assistant",
        "content": f"## 对话摘要\n\n{summary_text}",
        "is_compaction_summary": True,
    }
    
    # 返回：system + summary + 最近 N 轮
    system_msg = original_messages[0] if original_messages and original_messages[0].get("role") == "system" else {}
    return [system_msg, summary_msg] + recent_messages
```

### 改动 1.4：新增 _generate_summary 方法

```python
_SUMMARY_PROMPT = """你是一个对话摘要助手。以下是一段对话历史，请按主题分组总结。

要求：
1. 按讨论的主题分组，每个主题一段
2. 每段包含：讨论了什么、做出了什么决定/结论、是否有待办事项
3. 保留所有关键信息（文件路径、命令、决策、偏好），不要遗漏
4. 总长度不超过原始对话的 30%

## 对话历史

{messages_text}"""

def _generate_summary(self, messages):
    """用 LLM 对消息列表生成摘要。"""
    # 构建消息文本
    lines = []
    for msg in messages:
        role = msg.get("role", "unknown")
        content = _extract_content(msg.get("content", ""))
        if content:
            lines.append(f"[{role}] {content[:500]}")
    
    messages_text = "\n".join(lines)
    prompt = _SUMMARY_PROMPT.format(messages_text=messages_text)
    
    # 创建临时 LLM 客户端
    temp_client = _make_temp_client(self.llm)
    temp_client.messages = [{"role": "system", "content": "你是一个简洁准确的对话摘要助手。"}]
    temp_client.add_user_message(prompt)
    
    try:
        response = temp_client.chat()
        return (response.choices[0].message.content or "").strip()
    except Exception as e:
        logger.warning(f"摘要生成失败: {e}")
        return "（摘要生成失败，原始对话已被归档）"
```

### 改动 1.5：新增 _split_recent_turns 辅助函数

```python
def _split_recent_turns(messages, keep_recent_n):
    """将消息列表分为最近 N 轮和其余部分。
    
    一轮 = 从一条 user 消息到下一条 user 消息之前的所有消息。
    """
    # 找到所有 user 消息的索引
    user_indices = [i for i, m in enumerate(messages) if m.get("role") == "user"]
    
    if len(user_indices) <= keep_recent_n:
        return messages, []  # 全部是最近的
    
    # 分割点：倒数第 N 个 user 消息
    split_idx = user_indices[-keep_recent_n]
    
    older = messages[:split_idx]
    recent = messages[split_idx:]
    return recent, older
```

### 改动 1.6：更新 _build_remaining_messages 使用 keep_recent_n

```python
def _build_remaining_messages(messages, decisions, tool_refs, keep_recent_n=3):
    # ... 现有逻辑不变 ...
    # 只把 messages[-3:] 改为 messages[-keep_recent_n:]
```

## 任务 2：移除 Session Summary 机制

### 改动 2.1：session_manager.py — 删除 summary 生成和注入

**文件**：`src/core/session_manager.py`

在 `_create_session()` 和 `_reset_session()` 中，删除以下逻辑：
- 读取上一条 session 的 summary
- 调用 `generate_session_summary()`
- 注入 summary 到新 session 的 system prompt

简化为：创建新 session，不注入任何跨 session 信息。

### 改动 2.2：确认删除 session_resume.py（如果存在）

**文件**：检查 `src/core/session_resume.py` 是否存在，如果存在且只有 summary 生成功能，可以整个删除。如果还有其他功能，只删除 summary 相关函数。

### 改动 2.3：更新 config.yaml 解析

如果 `_build_compaction_config` 在 `session.py` 中，添加新配置项的解析：

```python
def _build_compaction_config(config, model_context_window=None):
    c = config.get("compaction", {})
    return CompactionConfig(
        # ... 现有字段 ...
        keep_recent_n=c.get("keep_recent_n", 3),
        summary_trigger_ratio=c.get("summary_trigger_ratio", 0.5),
    )
```

## 注意事项

- 不要改动 `src/core/adapters/base.py`（刚刚改过的错误分类）
- 不要改动 `src/core/agent.py`（刚刚改过的中断机制）
- `is_compaction_summary` 字段要在 JSONL 写入时保留
- `_make_temp_client` 已经在 compaction.py 中存在，直接复用
- 保持所有现有测试能通过
